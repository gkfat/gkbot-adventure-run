rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Infra baseline: lock down everything from client by default.
    // Server-side access uses Firebase Admin SDK and bypasses rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
